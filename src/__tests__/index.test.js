import Form, {
  Button,
  Checkbox,
  Dropdown,
  Group,
  Input,
  Radio,
  Select,
  TextArea,
} from '../'

describe('Form', () => {
  it('is truthy', () => {
    expect(Form).toBeTruthy()
  })
})

describe('Form.Button', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Button).toBeTruthy()
    expect(Button).toBeTruthy()
  })
})

describe('Form.Checkbox', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Checkbox).toBeTruthy()
    expect(Checkbox).toBeTruthy()
  })
})

describe('Form.Dropdown', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Dropdown).toBeTruthy()
    expect(Dropdown).toBeTruthy()
  })
})

describe('Form.Group', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Group).toBeTruthy()
    expect(Group).toBeTruthy()
  })
})

describe('Form.Input', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Input).toBeTruthy()
    expect(Input).toBeTruthy()
  })
})

describe('Form.Radio', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Radio).toBeTruthy()
    expect(Radio).toBeTruthy()
  })
})

describe('Form.Select', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.Select).toBeTruthy()
    expect(Select).toBeTruthy()
  })
})

describe('Form.TextArea', () => {
  it('is truthy', () => {
    // eslint-disable-next-line import/no-named-as-default-member
    expect(Form.TextArea).toBeTruthy()
    expect(TextArea).toBeTruthy()
  })
})
